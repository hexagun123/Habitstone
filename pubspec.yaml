name: flutter_skeleton
description: A minimalistic Flutter project skeleton
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.15.1
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  get_it: ^8.0.3
  fl_chart: ^1.0.0
  google_fonts: ^6.2.1
  go_router: ^16.0.0
  intl: ^0.20.2
  window_manager: ^0.5.1

  path_provider: ^2.1.5

dependency_overrides:
  source_gen: 2.0.0
  analyzer: 7.5.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  hive_generator: ^2.0.1

  # Code Generation
  build_runner: ^2.5.4
  riverpod_generator: ^2.6.5

  freezed: ^3.1.0
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
